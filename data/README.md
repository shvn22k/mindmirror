# Data

- **`processed/`** — generated by the pipeline (videos, landmarks `.npy`, feature `.parquet`, `clip_features.csv`, labels). This folder is **not** committed to Git (see `.gitignore`). Populate it from your AVCAffe (or compatible) layout and run the scripts under `scripts/`.
